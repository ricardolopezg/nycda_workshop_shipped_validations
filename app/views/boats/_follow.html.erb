  <%= form_for(current_user.active_relationships.build) do |f| %>
  <!-- , url: {action: "create", controller: "relationships"}, remote: true  -->
    <div><%= hidden_field_tag :followed_id, @boat.id %></div>
    <!-- <div><#%= hidden_field_tag :follower, :value => @current_user.id %></div> -->
    <!-- <#%= f.hidden_field :follower_id, :value => @current_user.id %> -->
    <%= f.submit "Track" %>
  <% end %>
     <!-- @relationship -->
     <!-- <#%=link_to "Track", relationships_path(followed_id: @current_user.id), method: :post, remote: true %> -->